<!-- content heading -->
<% if (title) { %>
    <h1 id="centered"><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>

<%- messages() %>

<body>
    <!-- One to trigger add new classification view -->
    <h2><a href="/inv/newClass">Add New Classification</a></h2>

    <!-- One to trigger add new inventory view -->
    <h2><a href="/inv/newInv">Add New Vehicle</a</h2>
</body>